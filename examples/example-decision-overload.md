# Example decision overload

## Raw input

I have too many paths at once.
I can see the product, the public narrative, the kernel, the agent interface,
and the business side, but they are all colliding and I cannot tell which one
should be made explicit first.

## Expected routing

- `nepm-kernel`
- `helpful-now-deriver`
- `decision-snapshot-builder`
